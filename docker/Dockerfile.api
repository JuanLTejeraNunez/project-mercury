# docker/Dockerfile.api
FROM python:3.12-slim
WORKDIR /app
COPY src/ src/
RUN pip install --no-cache-dir fastapi uvicorn requests
ENV PYTHONPATH=/app/src
EXPOSE 8000
CMD ["uvicorn", "control.api:app", "--host", "0.0.0.0", "--port", "8000", "--log-level", "info"]
