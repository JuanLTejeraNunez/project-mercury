# docker/Dockerfile.agent
FROM python:3.12-slim
WORKDIR /app
COPY src/ src/
RUN pip install --no-cache-dir requests
ENV PYTHONPATH=/app/src
# Default command runs market_brain in one-shot mode; override in compose if you want worker mode
CMD ["python", "-m", "src.core.market_brain"]
